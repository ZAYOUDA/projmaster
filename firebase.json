{
  "firestore": [
    {
      "database": "default",
      "rules": "firestore.rules"
    },
    {
      "database": "(default)",
      "rules": "firestore.rules"
    }
  ],
  "functions": {
    "source": "functions"
  }
}
