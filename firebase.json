{
  "firestore": [
    {
      "target": "dev",
      "rules": "firestore.rules"
    },
    {
      "target": "prod",
      "rules": "firestore.rules"
    }
  ],
  "functions": {
    "source": "functions"
  }
}
